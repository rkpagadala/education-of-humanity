# Coverage gaps — unregistered numeric tokens by section

Total unregistered tokens: 0 across 0 sections.

Each token needs a per-occurrence `reg(...)` bound to its section with a reason and source: data → wcde/wdi/checkin/derived; calendar/lookup year → `year`; definitional constant → `const`.
